# Parent handoff 2026-08-23

Next session should start here. Do not reopen Child 11.
Do not archive this parent until leftover domain defects are owned or Alan accepts them.

## Current product head

- Protected `origin/main`: `9406110d` `chore(main): release 0.74.0` (#434)
- Published tag: `v0.74.0`
- Image: `docker.io/linnea7171/houfeng:v0.74.0` (linux/amd64 + linux/arm64)
- Parent task: `.trellis/tasks/07-13-vps-detail-experience-design/` stays `planning`, `12/12`
- Child 11 is archived at `archive/2026-08/07-14-vps-records-integration-rollout/`
- No active Trellis child. Do not start a second Child 11.

The 2026-08-21 parent audit is a historical snapshot. Alpine/MinIO leftovers and unmerged Release Please #434 in that note are closed.

## Child 11 closed as

Integration owner only. Acceptance was assembly + gates, not “permanent delete on” and not “all 20 adapters present.”

| Merge | Note |
|---|---|
| #433 `79f62aac` | readiness + backup/restore packages/CLIs + gate scripts |
| #435 `ef71cc58` | archive Child 11 + parent 12/12 audit |
| #436 `38a5524d` | follow-up: musl-safe `0059` + S3 temporary key for portability staging |
| #434 `9406110d` | release 0.74.0 |

Official restore exists as packages/CLIs. It is **not** wired into the production readiness pair.

## Still blocking parent archive / permanent delete

Production HTTP stays `handlers.RecordDeletions(nil)`.
`HOUFENG_RECORDS_ENABLED` / `HOUFENG_PORTABILITY_ENABLED` stay default off.

Any one of these is enough to keep permanent delete disabled:

1. `deletion.record_markdown_client` missing — do not invent
2. `deletion.record_comparison` missing — do not invent
3. `recovery.record_search` missing
4. `recovery.record_collaboration` missing
5. `recovery.record_portability` missing
6. Production backup/restore pair absent (both present or both absent)
7. HTTP `RecordDeletions(nil)`

Return missing adapters to their owning children. Child 11 must not grow a markdown or comparison deletion adapter.

## Other parent leftovers (not Child 11)

- Child 7: overview manage-panel writes still live in `LegacyVPSDetail`
- Child 7: activity viewer allowlist is project-digest only
- Child 8: sticky row headers omitted under the CSS ratchet
- Child 8 mixed-load harness was parked on Child 11; Child 11 is closed, so any later harness is a new owned slice

## Hard constraints for the next agent

- Never checkout, commit, merge, or push local/remote `main`/`master`
- Named `store.AdmissionGate` only; no `AdmissionGateFunc` / allow-all in production
- `WitnessedRecordSubjectTombstoneSource` must not be nil in production
- store must not import portability; evidence must not import records or portability
- Root migrations: Child 11 added none; follow-up `0059` only relaxes `blob_key` CHECK
- Do not rewrite `0058`
- Production PDF binary is `houfeng-content-processor`
- Local host may be Go 1.27; official CI is `go.mod` `1.26.2`. Do not rewrite the PNG golden to pass local `verify-go`
- `GOTOOLCHAIN=local` on this host if the `go1.26.2` toolchain download fails
- Do not run `gofmt` and `go test` in the same sandbox; official gate is `make verify-go`
- `TMPDIR=/tmp`; Node 22. Any `--- SKIP:` in integration/recovery/capacity is a failure
- `recordreadiness` tests must not import `recordbackup`

## Suggested next slice

Create a new child only after Alan names the owner:

1. markdown/comparison deletion adapters, or
2. search/collaboration/portability recoveries, or
3. production backup/restore registry pairing after those adapters exist, or
4. accept the partial adapter set and archive the parent with permanent delete still off
