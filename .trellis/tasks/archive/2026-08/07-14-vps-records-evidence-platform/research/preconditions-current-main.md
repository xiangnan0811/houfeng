# Research: Child 4 execution preconditions on current main

- Query: Verify Child 4 preconditions against the supplied current-main checkout at `51c87355`: Child 1/2/3 merged state, migration numbering and `0054` availability, APP ACL/convergence/runtime admission, and ordering constraints.
- Scope: internal
- Date: 2026-08-10

## Findings

### Predecessor delivery state

- Child 1 foundation is archived with `status: completed`, `completedAt: 2026-08-02`, and protected-main merge evidence in [Child 1 task.json](/home/murray/code/houfeng/.trellis/tasks/archive/2026-08/07-14-vps-records-platform-foundation/task.json:1) (notes cite PR #394 and protected-main commit `2cbeb1bb...`).
- Child 2 records core is archived with `status: completed` and `completedAt: 2026-08-04` in [Child 2 task.json](/home/murray/code/houfeng/.trellis/tasks/archive/2026-08/07-14-vps-records-core/task.json:1). The current tree contains its `0052_create_records_core.sql` and records stores/tests; current APP contract registers the nine core tables plus `validate_record_revision_primary_subject()` in [app_acl_current_contract.go](/home/murray/code/houfeng/internal/center/store/migrate/app_acl_current_contract.go:33).
- Child 3 attachments is archived with `status: completed` and `completedAt: 2026-08-09` in [Child 3 task.json](/home/murray/code/houfeng/.trellis/tasks/archive/2026-08/07-14-vps-records-attachments-storage/task.json:1). The current tree contains `0053_create_record_attachments.sql`, attachment stores/Blob adapters, and [record_attachment_participant.go](/home/murray/code/houfeng/internal/center/store/record_attachment_participant.go:14). Child 3's current fragment is registered in [app_acl_current_contract.go](/home/murray/code/houfeng/internal/center/store/migrate/app_acl_current_contract.go:109), and source/ACL/PostgreSQL tests cover it (for example [record_attachments_app_acl_test.go](/home/murray/code/houfeng/internal/center/store/migrate/record_attachments_app_acl_test.go:10)).
- The parent plan's migration/order table assigns Child 4 to `0054_create_record_evidence.sql` after Child 2=`0052` and Child 3=`0053` ([parent implement.md](/home/murray/code/houfeng/.trellis/tasks/07-13-vps-detail-experience-design/implement.md:50)). The parent narrative still contains stale `1/11` progress text ([parent implement.md](/home/murray/code/houfeng/.trellis/tasks/07-13-vps-detail-experience-design/implement.md:21)); do not use that text as current merge-state evidence.

### Migration numbering

- The current `db/migrations/` inventory has the frozen `0001`-`0051` prefix, `0052_create_records_core.sql`, and `0053_create_record_attachments.sql`; no `0054` file exists. Thus `0054_create_record_evidence.sql` is available and is the next required number. The duplicate `0004_*` names are historical and intentional; do not renumber them.
- The database guideline's generic migration paragraph is stale: it still describes the current root as ending at `0052` and says `0053` is the next candidate ([database-guidelines.md](/home/murray/code/houfeng/.trellis/spec/backend/database-guidelines.md:14), [database-guidelines.md](/home/murray/code/houfeng/.trellis/spec/backend/database-guidelines.md:77)). The parent and Child 4 artifacts explicitly rebaseline to `0054`; use the actual embedded inventory and parent table when implementing.

### APP ACL, convergence, and runtime admission

- Production current fragments are a closed-world registry. `appACLCurrentMigrationFragments` currently contains exactly core and attachments ([app_acl_current_contract.go](/home/murray/code/houfeng/internal/center/store/migrate/app_acl_current_contract.go:28)); adding `0054` requires adding exactly one fragment for the exact filename. `compileAppACLCurrentSourceContract` snapshots embedded SQL, validates the frozen R1 prefix, rejects missing/extra/duplicate fragments and invalid managed objects/privileges/functions before any transaction begins ([app_acl_current_contract.go](/home/murray/code/houfeng/internal/center/store/migrate/app_acl_current_contract.go:202)). Privilege callbacks are evaluated once with a fixed validation DB name and materialized defensively ([app_acl_current_contract.go](/home/murray/code/houfeng/internal/center/store/migrate/app_acl_current_contract.go:273)); catalog compilation only substitutes the real database name.
- `ConvergeAppACLCurrent` compiles/validates first, then runs a `SERIALIZABLE` transaction ([app_acl_current_convergence.go](/home/murray/code/houfeng/internal/center/store/migrate/app_acl_current_convergence.go:62), [app_acl_current_convergence.go](/home/murray/code/houfeng/internal/center/store/migrate/app_acl_current_convergence.go:93)). It supports only fresh exact-current state; fresh applies the exact embedded set, DCL, catalog verification, and one genesis manifest, while exact repeat verifies read-only. Legacy successor/upgrade/repair states return `ErrDevelopmentDatabaseRebuildRequired` ([app_acl_current_convergence.go](/home/murray/code/houfeng/internal/center/store/migrate/app_acl_current_convergence.go:183)).
- `AdmitAppACLCurrentRuntime` compiles the same current source contract before opening a transaction, then uses exactly one `REPEATABLE READ READ ONLY` transaction to verify the persisted manifest/ledger and effective catalog ([app_acl_current_runtime_admission.go](/home/murray/code/houfeng/internal/center/store/migrate/app_acl_current_runtime_admission.go:21), [app_acl_current_runtime_admission.go](/home/murray/code/houfeng/internal/center/store/migrate/app_acl_current_runtime_admission.go:42)). It does no DDL/DCL and has no fallback to owner migration.
- Configuration selects legacy only when both record flags are false. `HOUFENG_RECORDS_ENABLED=true` with permanent-delete false selects runtime admission; `false/true` and `true/true` fail before other configuration/secret reads ([config.go](/home/murray/code/houfeng/internal/center/config/config.go:71)). Center bootstrap applies legacy migrations only in legacy mode; runtime mode admits before constructing repositories ([bootstrap.go](/home/murray/code/houfeng/cmd/houfeng-center/bootstrap.go:68)). The VPS importer follows the same runtime admission-before-repository path ([main.go](/home/murray/code/houfeng/cmd/houfeng-import-vps-json/main.go:220)).

### Ordering and integration constraints for Child 4

- `0054` DDL, its exact `AppACLCurrentMigrationFragment`, managed-object/privilege declarations, and convergence/runtime-admission tests must be one atomic delivery. A migration without a fragment fails closed before `BeginTx`; a fragment for an absent/duplicate migration is likewise rejected.
- Do not modify frozen `0001`-`0051`, `0052`, or `0053`; do not provide old-database upgrade/backfill. Fresh/current exact repeat is the only supported database state. A development database without `0054` must be rebuilt, per Child 4 rebaseline ([prd.md](/home/murray/code/houfeng/.trellis/tasks/07-14-vps-records-evidence-platform/prd.md:7), [implement.md](/home/murray/code/houfeng/.trellis/tasks/07-14-vps-records-evidence-platform/implement.md:13)).
- Existing records revision commits use a deterministic `RevisionParticipantRegistry`, sorted by participant name, and invoke each participant inside the caller-owned transaction after core revision/projection writes ([revisions.go](/home/murray/code/houfeng/internal/center/records/revisions.go:219), [records.go](/home/murray/code/houfeng/internal/center/store/records.go:26), [records.go](/home/murray/code/houfeng/internal/center/store/records.go:150)). An evidence participant must obey that contract (no network calls) and be registered alongside the existing attachments participant; failures roll back the whole revision transaction.
- The production `newRecordsHTTPHandlers` wiring currently passes a nil `AdmissionGate` to Records, drafts, and platform repositories with an explicit comment that the concrete production gate belongs to Task 10 ([bootstrap.go](/home/murray/code/houfeng/cmd/houfeng-center/bootstrap.go:296)). The repository rejects nil/unavailable gates before writes ([record_platform.go](/home/murray/code/houfeng/internal/center/store/record_platform.go:727)). Child 4 can build and test evidence services/handlers, but production capture/save remains intentionally fail-closed until the later gate owner is wired; do not weaken this gate or imply evidence capability is live.
- Child 5 is explicitly ordered after Children 2, 3, 4, and 9 ([parent implement.md](/home/murray/code/houfeng/.trellis/tasks/07-13-vps-detail-experience-design/implement.md:122)); Child 4 should leave public contracts stable for those consumers and must not start them implicitly.

## Caveats / Not Found

- No `0054` migration or evidence fragment currently exists, as expected for the planning task; this is the implementation starting point, not a missing predecessor.
- Child 2/3 task metadata does not consistently record merge commit/PR fields (Child 2/3 `commit` and `pr_url` are null), so predecessor confirmation relies on their archived `completed` status plus the checked-in migrations, stores, participants, and ACL/integration tests visible in the supplied current-main tree.
- The database guideline has not yet been updated from its `0052`-ending wording; treat the parent/Child 4 rebaseline and actual `db/migrations` inventory as the operative contract.
