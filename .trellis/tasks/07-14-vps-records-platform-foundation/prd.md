# 统一授权与平台基础

## Goal

建立记录平台统一授权、outbox/worker、删除账本、恢复信任与备份恢复控制面的 fail-closed 基础。

## Requirements

> **2026-07-24 r1 governance correction — takes precedence over earlier projector-ACL wording in this parent.** The `records-on/delete-off` child `07-24-app-acl-migration-runtime-handoff` freezes r1 runtime and platform-admin persistent-function `EXECUTE` sets as empty. The two 0051 projectors remain migrator-owned catalog objects and are verified by admission, but no current APP role may invoke them until a future trusted caller, witness binding and independent admission are designed. r1 catalog convergence is limited to its migration-owned APP surface; it does not reject unrelated schemas or unrelated owners' default ACLs.

> **2026-08-02 published-migration and APP V3 correction — authoritative for all remaining foundation work.** `db/migrations/0051_create_record_platform_foundation.sql`, `db/appaclr2/migrations/0052_app_acl_r2_privileged_transition.sql`, and the three isolated-domain `0001` migrations are released artifacts and are immutable byte-for-byte, including whitespace. Children 2–11 retain root migration numbers `0052–0060`; foundation successors are exactly `0061_upgrade_record_platform_foundation_to_app_acl_v3.sql`, `0062_add_domain_identity_provision_authority_gate.sql`, and `0063_add_app_acl_v3_readback.sql`. The first successor in each isolated migration namespace is exactly `0002_add_record_platform_governance.sql`; later isolated changes use the next unoccupied additive `0002+` filename. The old draft schema is not a suffix of released 0051, so V3 is one forward-only released-R2-to-V3 transition, not an in-place rewrite or a grant-only patch.

- 父设计：`../07-13-vps-detail-experience-design/design.md` §10、§11.3–11.4、§15、§18、§21、§23。
- 直接依赖：无；这是其余 10 个子任务的安全与运行时前置。
- 扩展当前只有 `admin` 的 session context，使服务端能够统一获得 actor、role、未来 group 与默认 project scope；不在前端伪造权限。
- 提供唯一的 `recordauth.Policy`，覆盖记录、草稿、证据、附件、搜索、活动、比较、通知、导入导出及永久删除 capability。
- 建立通用 outbox、幂等请求、identity mutation guard、对象版本/lease 和后台 job 基础；网络调用不得位于业务事务内。
- 应用数据库只保存 reservation、operation 投影和最小审计；`delete_commit|attempt_not_committed|contract_activation|domain_identity_rotation` 位于独立 DSN、独立迁移和独立恢复域的 append-only ledger。
- 至少实现 `postgres_sync` full witness；`postgres_sync` 使用第四个独立 PostgreSQL 恢复域，`s3_worm` 使用三个 PostgreSQL 恢复域加独立 S3 bucket/namespace，未选 profile 的凭据不得被读取。S3 WORM 保存完整 canonical entry 与逐序列 immutable head receipt，使用 Object Lock COMPLIANCE 与 legal hold，不允许只保存可覆盖 head/hash。
- 建立 `RecoveryTrustStore`、版本化签名 `RecoveryPointManifest`、recovery inventory、backup/restore attempt/workspace 与连续 deletion replay checkpoint 基础。
- 首个 recovery trust root 不得由普通 center 启动时对空库执行 TOFU，也不得靠手工 SQL。提供独立 `houfeng-record-platform-admin` 的确定性 `activation plan/apply` 仪式：`plan` 只读盘点恢复源并固定预期 head、候选公钥、initial signed inventory/activation manifest 与 contract activation；`apply` 必须核验完整 plan digest 和批准策略，按可恢复 saga 写 primary、full witness、签名库存、ledger activation 与 projection。
- 自动化通过 detached Ed25519 approval file 和显式 bootstrap approver policy 完成；离线 `approval sign` 不连接任何数据库，GitOps 只消费不晚于 plan 到期的短期 plan/approval artifact，不把 recovery/approver 私钥写入仓库。不提供 `--yes`、digest 前缀或自由文本 actor 作为授权。approval envelope 逐字绑定 `current|candidate` scope 与对应 policy digest；同一签名不得跨 scope 双计。所有 trust mutation 使用稳定 `mutation_id + plan_digest` 幂等身份；ack 丢失后只续跑原 canonical bytes，不得生成第二个 trust revision 或 activation。
- 首次 durable intent 必须把 bounded canonical plan bytes 与本次二选一授权模式的 canonical authorization envelope 一起固化为 immutable authorization artifact；detached 模式保留完整、规范化排序的 scoped approval envelopes，TTY 模式保留 policy 允许位、完整 plan digest 确认和由受信任 OS credential 映射派生的 canonical operator principal。primary 与 full witness 都逐字保存并绑定 artifact digest，因本地文件或 recovery-control 丢失而续跑时可仅凭 `mutation_id` 恢复；任何模式、principal、approval 或 byte 变化都冲突。artifact 永久禁止 record/object ID、内容、自由文本、私钥、路径或 argv，30 天后只清 parsed attempt/approval operational detail，不清这份非秘密治理证据。
- key add/rotation/retire/compromise/remove 与 approval-policy rotation 复用同一 mutation envelope。正常轮换需要旧 active key 授权与新 key possession proof；compromise 使用 activation 时锚定的离线 approver policy；approval-policy rotation 同时满足旧 policy threshold 与新 policy threshold/possession proof。进入 `removed` 终态前必须有签名依赖库存证明为 0。历史 witness 中的非秘密 public key bytes 继续作为不可变审计，但运行时不得再用它验签或复活同 key ID。
- PostgreSQL runtime/admin 不直接获得 ledger/trust/witness immutable entry `INSERT` 或 head `UPDATE`；migrator-owner 创建固定 entry kind、expected head/sequence 与 canonical digest 的窄 `SECURITY DEFINER` 函数；每个可授权持久 mutator 只接受一个 bounded canonical `bytea` command，ACL identity 固定为 `public.name(bytea)`，禁止裸函数名、额外 overload 或多参数便利入口。函数固定 `search_path=pg_catalog`、撤销 PUBLIC 权限，runtime/admin 只获得各自完整 identity 的 `EXECUTE`。S3 的共享读权限可以重叠，但 runtime deletion、admin activation/trust 与 immutable head receipt 的写 prefix 必须不重叠，并以双向越权测试证明。
- 不以 DSN 字符串不同冒充恢复域独立。`DeploymentIDV1=dp-[0-9a-f]{64}`、v1 `ProjectIDV1=default`，migrator 记录 canonical PostgreSQL system identifier/database identity 与随机 domain ID；托管服务 fallback 把不可变 stable physical identity 与可续签、单调 generation 的短期部署治理 attestation 分离。S3 第三组 read-control-only probe 的 raw endpoint/provider/account/cluster/storage/snapshot/restore inputs 只存在于本次运行；持久化合同只保存 `HTTPSAuthorityV1` digest、TLS/SPKI、adapter kind+normalization version、六个 field-domain-separated infrastructure digests、`S3BucketNameV1`、`S3NamespaceV1` tuple+digest、versioning/Object-Lock/retention/replication stable fields。raw URL/path/query/自由文本不进入不可变治理。preflight 对选定 profile 做 pairwise distinct、expected-kind 和应用备份位置不重叠校验。active profile 的 canonical identity set/digest/epoch 必须进入 plan、sequence-1 activation、projection 与 full witness；无法证明、attestation 到期或 live probe 与 witnessed identity 不符时永久删除保持关闭。
- PostgreSQL/S3 stable identity、liveness/possession attestation 与治理 trust policy 使用唯一 canonical v1 field order、类型、大小、时间和签名合同；strict regular/no-follow/bounded loader、threshold、key ID、排序、轮换与 witnessed policy digest 均不可留给实现自行解释。renewal 只更新 liveness，不改变 stable set digest。
- 激活后在当前 profile 内合法替换一个物理域或执行灾难恢复，必须走 `domain identity rotate` 管理 saga，而不是改配置后自动接受。candidate admission 固定为 authenticated `challenge draft → current threshold sign/seal → prepare → candidate attestation/preparation threshold sign/seal → plan`；offline signer 不连接数据库/网络，prepare 不持 governance private key，candidate 使用独立 pinned-current policy，任一单签名阈值绕过均拒绝。完整 sealed challenge 与 preparation canonical bytes 必须进入 typed `DomainRotationIntentV1`、primary 与 full witness；durable intent 后即使本地 artifact 和 recovery-control 均丢失，current仍能按mutation ID从primary/full witness重建typed recovery-request；exporter必须用policy中witnessed current request signer做`candidate_recovery_request_v1` purpose-bound签名，先完成primary/full-witness 2+3 immutable publication/readback，再把含完整publication receipts的request经FD交给candidate，外部文件只能作为可选 exact-match 输入。candidate identity-set epoch 必须等于 current+1，且一次只允许一个 member 改变；`DomainIdentitySetBodyV1`、`IdentitySetPrimaryReceiptV1` 与显式绑定 primary receipt digest 的 `IdentitySetWitnessReceiptV1` 是三个独立、闭合、可穷尽解码的 schema，PostgreSQL/S3 full witness 都必须保存足以从 primary 丢失中重建的完整 bytes，不能保存无类型 blob。current approval threshold 与 candidate domain 的 live possession/governance proof 均满足后，从 genesis 逐字复制 ledger/trust/full-witness/authorization artifacts，完成受管数据 catch-up、append-only 双写或经独立 `unreachable draft → governance sign → seal → resume` 生成并 witnessed 的灾难不可达证明、drain 和严格 `cutover export → candidate apply → current resume --receipt-fd FD` 回流，再以新的判别式 ledger entry 与 full witness 推进 deployment contract。transfer、cutover与recovery-request exporter 必须显式读取 `--recovery-signing-key PATH` 指向的 bounded regular/no-follow exact-0400 raw Ed25519 key，并在输出第一个字节前 exact-match 对应 witnessed transfer/request signer；missing/unreadable/symlink/wrong-mode/wrong-length/malformed/wrong-signer 或 expired descriptor/deadline 时输出字节数为 0。recovery-request signed core 上限 32 MiB、每份 publication receipt 上限 64 KiB、最终 FD wrapper 上限 33 MiB，所有 decoder 在 allocation 前拒绝 +1 byte；合法大对象使用 `ObjectStart → ObjectChunk* → ObjectEnd`，逐 object 绑定 digest/length、连续 offset 与 index/count，plan/bundle/inventory 上限分别为 24/20/8 MiB。receipt 顺序固定为 `DrainContinuation* → CandidateImportApplied → CandidateImportRevoked → Cutover`；`cutover_apply` request的required head必须是witnessed `Cutover` receipt，且其`PreCutoverReceiptChainHead`逐字绑定内嵌`candidate_import_revoked`；15 个闭合 kind 中仅 `DrainContinuation` 可重复，其余 14 个均为 singleton。candidate import 完成后必须先生成 preparation-key-signed `candidate_import_applied`，由 current-side append/full-witness/readback 后才允许显式撤销 import credential并 witness `candidate_import_revoked`，随后才允许构造 cutover bundle。candidate receipt 只能由 current-side resume 验签、append、full-witness 并 read back，candidate 不能写 current recovery-control。完成顺序固定为 `Final witnessed → revoke cutover credential → current primary/full-witness/readback revocation → destroy candidate receipt key/bundle/workspace与AEAD/nonce key material → witness purge/workspace-zero receipts → completion receipt（绑定最终 receipt-chain head、candidate-control policy-head generation/digest及最终primary/witness publication receipts）→ complete`；revocation readback前销毁成功数为0。durable intent 后只可修复并续跑；旧域 retirement 后不能回退或重新获得 admission。缺失足以重建的 current full witness/受支持恢复源时保持失败关闭。`postgres_sync ↔ s3_worm` profile 改变不是单域轮换，v1 明确拒绝并要求独立 profile-transition 设计，不能被当作普通恢复静默接受。
- candidate-control 在 challenge/prepare 前必须完成可执行的 `read-only control-policy draft（创建 mutation ID）→ current+candidate governance offline sign → seal → current primary/full-witness publish`；`domain governance sign --purpose candidate-control-policy --scope current|candidate`是该policy唯一离线签名入口；generation-1 draft 必须显式读取公开、canonical 的 `--nonce-reservation-signer-descriptor` 与 `--cleanup-verifier-descriptor`，并从current witnessed trust读取只允许`candidate_recovery_request_v1`的request-signer descriptor，而同 phase renewal 及 `prepare→import→cutover→cleanup` 前进只能从 witnessed `--previous-policy` 逐字继承两者并在 stat 前拒绝重新注入 descriptor。challenge draft先对完整body执行monotonic challenge-start CAS并完成typed primary/full-witness 2+3 fence publication/readback，再输出并逐字绑定published prepare policy，prepare/import/cutover/cleanup四个policy phase每次写第1字节前都exact-match对应已witnessed generation及live control-plane。policy chain只允许same-phase renewal或相邻phase前进，generation单调并绑定previous digest。nonce reservation signer、phase receipt signer、cleanup verifier与current recovery-request signer四者分离：独立nonce key只签purpose `candidate_aead_nonce_reservation_v1`的两类encrypted-payload reservation，candidate receipt key只签import/cutover phase receipts；current request signer只签完成2+3发布的recovery-request；独立one-shot verifier用外部strict-0400 Ed25519 key、read-control identity和live filesystem exclusion proof，只签`candidate_artifacts_purged|workspace_zero`，私钥不进入center/admin/candidate/env/control/workspace/backup。AEAD nonce reservation以domain-separated `(mutation,key identity,nonce)`物理唯一key做S3 conditional create/filesystem O_EXCL/PostgreSQL unique insert；artifact只引用reservation digest。wrapped DEK是单独`encrypted_key_material` semantic；恰有六个byte-handling命令——candidate prepare、transfer import、cutover apply、credential revoke、candidate abandon、candidate cleanup——的parser exact-one接受strict-0400 raw 32-byte local key或pinned KMS config+strict-0400 credential file，其他scope在stat前拒绝，local/KMS descriptor与destruction receipt均exact XOR。nonce private key仅prepare/import/cutover可调用签名；credential revoke不能打开，abandon/cleanup仅可定位、核验并销毁且signer调用数为0。完整 nonce/cleanup/request-signer descriptors 必须进入 published policy、challenge fence、sealed preparation 与 typed intent；Final绑定完整policy-chain head和cleanup-verifier descriptor；Final后primary/PostgreSQL-witness/S3禁止替换verifier key/public identity/exclusion-proof/validity。任一key/policy/control-plane/nonce/signer漂移都保持fail closed，completion还必须绑定policy-chain head及最终primary/witness publication receipt digests。
- 首次 sequence-1 activation 前，部署必须先把 LB 切到 exact-204 probe、停止/排空 legacy target 与记录队列消费者，并生成两份 policy-bound `SignedAdmissionSnapshotV1`；短期 drain receipt 逐字内嵌完整 LB/queue wrappers、policy、deployment/project/epoch/minimum version、config、fresh target/connection/consumer/lease inventories 与零计数，`activation plan` 固定该 receipt/scope digest。首次 durable intent 前原 receipt/plan/approval 必须有效且 live state exact-match；durable intent 后只允许相同 mutation 用内嵌两份 fresh signed wrappers、fresh inventories及 previous digest 的 continuation证明同 scope/config 仍为零后续跑，完整 receipt进入 primary/full witness，本地 snapshot 或原 artifact 后来到期/清理不阻断 exact reconciliation。旧目标重新出现时暂停，重新排空后继续同一 mutation。允许计划内记录域短暂停流，不允许 sequence 1 后再依赖旧 `/ready` 慢慢收敛。
- 配置或外部依赖未知时只关闭记录永久删除/受保护内容域，不阻断不含记录内容的监控采集；低于 minimum fence-contract version 的实例不得接流量/任务。
- APP R1/R2 的版本化 allowlist 保留现有 `0001…0051` 迁移证据、22 张 0051 public 表、4 条 sequence `USAGE` 与独立单调 manifest chain，但 runtime/platform-admin 的持久函数 `EXECUTE` 集精确为空；0051 的两个 projector 只是 migrator-owned、PUBLIC-revoked 的历史 catalog evidence。V3 必须在已发布 R2 transition advisory lock 下，由受限 direct migrator 启动 `SERIALIZABLE READ WRITE` 闭包，只接受 exact `FINALIZED`，再由短期 bootstrap provisioner 按 `schema_owner`/`definer_owner` 两个 NOLOGIN owner phase 原子应用 0061–0063、转换数据、收敛 V3 ACL/authority、追加 V3 evidence/head 并完整 readback。R2 `R1|PREPARED|CORRUPT`、partial 或 drift 都零写拒绝；commit ACK 丢失只可通过 exact V3 classification 成功，不得重放 DDL。提交后 V3 runtime admission 成功，released R1/R2 admission 必须拒绝 successor；正常 migrator/runtime/admin/validator 均不拥有数据库对象或 owner membership。后续 child migration 仍按 filename/checksum 完整集与独立 manifest revision 单调推进，不能由 migration 编号推导 revision。
- 普通日志/trace 禁止 record/object stable ID 与正文；详细 operation/lease/job 元数据服从父设计 24 小时/30 天压缩规则。Retention 使用八个独立 registry roots；其中 `CanonicalSchemaRegistryV1`（只能由生产 bounded encoder/decoder/storage codec 登记真实 leaf）与人工审阅 `RetentionPolicyRegistryV1` 是两个分类权威，`RetentionLifecycleRegistryV1`只登记可复用clock template，policy stage binding固定具体anchor/expiry、participant、exact capability tuple、proof set、survivor与stage residue，`PurgeParticipantRegistryV1`固定owner/exact tuple capability/binding且与Go/Web executable dispatch全等，其余roots只登记S3/filesystem/client inventory grammar。初始程序清单精确为21个lifecycle和24个participant，由first-owner child按merge ordinal add-only引入。`RetentionSurfaceAddressV1` exact-one覆盖 PostgreSQL column、canonical leaf、S3 key/metadata/control property、managed file和managed client leaf；class明确区分live authority/derived、draft、managed-client与immutable/minimal/operational/recoverability/storage/ephemeral，正常产品内容只能在owner存续的live/draft/client class，transition后survivor必须为none。`ManagedClientStorageRegistryV1`只作为canonical producer；v1 `IndexedDBDraftBufferV1` same-origin、≤256 KiB、≤24h、无附件/证据bytes，local/session storage、CacheStorage与service-worker cache内容命中为0，offline设备只披露ack-or-expiry。生成器只exact join，不由字段名/类型/decoder猜语义。任一row-level独立surface/action/proof/schema数组都不得授权Cartesian product；未绑定stage只表示不适用，每个template stage全局至少有一个reverse consumer。telemetry的绝对clock以`captured_at`而非ingest/created时间为锚，S3 multipart与phase credential authority分别由typed abort/revoke action和zero proof闭合。manifest必须逐 PostgreSQL 普通/generated/storage-only列、canonical leaf、S3 property、managed filesystem与managed client surface双向全等；core dump逐层证明或登记为受管surface。未知enum/opaque bytes/未分类新列/额外对象/无法证明排除均关闭永久删除。
- Foundation 同时交付留存 registry 的两阶段供应链工具与唯一 owner matrix 编译入口。policy 固定 repository numeric ID `1232752877`、GitHub Actions check issuer App ID `15368`、merge order `1,2,3,4,9,5,6,7,8,10,11`，以及按 raw bytes 排序的精确五个 feature-PR contexts：`docker-image`、`go`、`record-retention/source-claim`、`web`、`web-browser`；三个 `record-platform-pg16-catalog (postgres:16.x)` contexts 属于独立 PG16 ruleset，不得并入这五项。`acceptance-policy.v1` 与 `genesis-approver-policy.v1` 是 atomic authority pair，缺失、partial replacement 或 residual provisioning marker 均 fail closed；genesis threshold 精确为 2，approver key ID 使用 `gapk-sha256-*`，acceptance policy 保留 active/retired/compromised key history。生产 verifier 只把完整 policy/receipt chain 解码成不可伪造的 opaque verified-history capability，caller 不得注入预解析 history。每个 child 的 PR/merge-queue required check 只生成确定性 `ChildRetentionSourceClaimV1`；首轮 11 个 child 全部要求 `registry_delta|required_nonempty`。feature 以双亲 merge commit 合入后，受保护主线使用 policy-pinned scanner 重扫实际 merge tree，再由不执行 merge-tree 代码的隔离 signer 以专用 Ed25519 key 生成 `SignedChildRetentionMergeAcceptanceV1`。完整 claim+acceptance 必须经 exact-one-file metadata-only PR 持久化到 `attestations/record-retention/v1/<ordinal>-<slug>/<feature-merge-sha>.acceptance.v1`，下一 child 等待该 PR 合入；CI artifact 不作为长期证据。Task 11 PR 只验证 1–10 acceptance 与自身 claim，Task 11 merge 后再产生第 11 份，父最终门验证连续 `before + delta = after` chain 与最终 inventory。开发分支/普通 runner 无签名私钥，Task 11 不能替其他 owner 补 registry。
- generation-1 candidate policy 还固定不可延长、至多30天的 mutation deadline，nonce/cleanup/request-signer三份 descriptor 必须覆盖该deadline，后续policy逐字继承；challenge后过期是terminal fail-closed，不能借renewal换signer。所有candidate命令只信任current从primary/full witness重建、由policy-pinned current signer做purpose-bound签名并完成2+3 immutable publication/readback的typed recovery-request FD，本地policy/preparation只能可选exact-match。pre-intent放弃使用独立challenge-state-aware no-intent fence→2+3 authorization→candidate zero→purge/workspace-zero→内嵌完整signed purge/workspace-zero receipts的2+3 abandon completion，不伪装成final-proof cleanup phase或第16种rotation receipt。import/cutover credential均由显式revoke命令经FD返回candidate-receipt-key签名，current append/full-witness/readback前不得销毁该key、bundle或workspace；cleanup只能在cutover-revoke ack后销毁。AEAD/wrapped-DEK/nonce销毁证据是不另签名的typed facts，由cleanup verifier仅有的purge/workspace-zero签名覆盖，两个remaining-key count必须为0。Final canonical body携带完整policy head+cleanup descriptor；primary/witness completion与S3 decoder显式携带policy-head generation/digest及最终publication receipts。
- 当前任务不实现 records/evidence/attachment 业务表、UI 或真正备份复制；只提供明确接口和可由后续适配器接入的运行时底座。

## Acceptance Criteria

> **2026-07-24 r1 acceptance correction — takes precedence over earlier 0051 function-grant text below.** r1 has 204 compiler tuples and no runtime/admin persistent-function `EXECUTE`. It uses a direct, constrained migrator, opaque PG16 `pgcrypto` extension members behind a no-`USAGE` internal schema, and a scoped catalog verifier. Any later projectors caller is a separate child admission problem.

- [ ] `PF-AC-001` middleware/session scope 与 `recordauth.Policy` 在 API、store query builder 和 worker 三类调用中给出同一授权结果；visibility/source authorization 使用版本化 typed scope、稳定 role/group、规范化 digest 与可验证 final floor，任意字符串、未知 capability/kind/version、缺失或篡改 floor 默认拒绝。
- [ ] `PF-AC-002` 当前 admin 映射 project admin；测试中的非 admin/group scope 能验证 allow/deny 交集，且未授权资源外部统一 404。
- [ ] `PF-AC-003` outbox 与业务事实同事务写入；worker 每次发送/重试重新授权。outbox/idempotency/guard claim 都绑定 owner ID + 单调 generation + live expiry，过期接管后旧 owner finalize 成功数为 0；幂等 TTL 不早于 owner lease，同 key 同请求返回原结果、不同请求稳定 409，janitor 不删除 live owner。
- [ ] `PF-AC-004` `postgres_sync` 的 app DB、primary ledger、full witness、recovery-control 使用四个 DSN/迁移账本；`s3_worm` 使用 app/ledger/recovery 三个 DSN/迁移账本和独立 locked S3 witness。配置测试拒绝 alias、相同 PostgreSQL system identifier/stable attestation digest、过期/回退/wrong-kind attestation、相同或应用备份重叠的 S3 identity、witnessed identity-set rollback、inactive-profile 凭据读取或不满足 TLS/retention 的生产配置；exact matrix 显式包含唯一 witness mode 并证明未选 profile 的变量/文件/endpoint 从未 stat、parse、resolve 或 open。
- [ ] `PF-AC-005` center runtime、platform admin、validator、normal migrator 与短期 bootstrap provisioner 使用不同数据库身份和环境文件；四个 operational roles 均不拥有数据库对象、没有 owner membership，center 不持 admin/migrator/bootstrap DSN。已发布 0051、APP R2 0052 与三个 isolated `0001` 的 SHA-256 逐字保持基线值；任何 foundation DDL 只进入 0061–0063 或 isolated `0002+`。APP R1/R2 的 runtime/admin persistent-function `EXECUTE` 精确为空，两个 0051 projector 仍为不可调用的 migrator-owned catalog evidence。真实 PostgreSQL 16.0、16.6、16.12 upgrade fixtures 从 exact R2 `FINALIZED` 出发，在现有 R2 transition lock 与一个 `SERIALIZABLE READ WRITE` closure 中完成 0061–0063、owner transfer、V3 manifest/head 与 exact readback；R2 非 FINALIZED/partial/drift 零写失败，任一 DDL/data/ACL/head/readback failpoint 全回滚，lost ACK 只由 exact V3 observer 收敛。V3 role graph固定为 NOLOGIN `schema_owner`/`definer_owner` 与零 ownership 的 caller roles；bootstrap credential commit 后被撤销且残留会阻断 readiness。V3 runtime 只读验证完整 chain/catalog 后成功，released R1/R2 admission 对 V3 successor 拒绝。数据库 direct immutable-entry/head 写入、跨角色 entry-type 写入、owner `SET ROLE` 与 broad grant 成功数均为 0；S3 center/admin 凭据与写 prefix 同样分离。
- [ ] `PF-AC-006` append ack 丢失、primary/witness 不一致、head 回退、序列缺口、far tail、未知 entry type/version 均保持 fail closed，不能开始 purge 或恢复读取；包括“对象/行已存在”快路径在内，每次 confirm 都逐字读取目标并从 genesis 验证到 immutable tail，不能只比较 head/hash。
- [ ] `PF-AC-007` `attempt_not_committed` witness durable 前不解除 reservation；durable 后同 key 永远返回未删除结果且不占对象 delete 唯一约束。
- [ ] `PF-AC-008` RecoveryTrustStore 可只靠 full witness 重建完整非秘密 trust chain；retired/compromised/missing key 语义符合父设计。
- [ ] `PF-AC-009` fresh revision-0 trust store 保持 fail closed；`activation plan` 无写入且输出确定性完整 digest，并绑定仍有效且证明 legacy target/connection/consumer/lease 已排空的 drain receipt 与 active domain identity set；`apply` 只接受完整 digest + 策略允许的本地 TTY 批准或 detached threshold approval。首次 intent 前过期拒绝；intent 后原 approvals/receipt 到期不阻断 exact reconciliation，但每次续跑需要同 scope/config 的 fresh zero-count continuation。canonical plan/authorization artifact 在 primary/full witness 逐字可恢复，删除本地文件和 recovery-control 后可仅按 mutation ID 重建并续跑；primary/witness/manifest/inventory/ledger/projection 任一 cutpoint 强杀后，同 mutation 可幂等续跑或安全保持未激活，普通 center 自动 seed 与手工 SQL 成功路径均为 0。
- [ ] `PF-AC-010` activation commitment 是无环 DAG：plan/bundle 都不含 plan digest、authorization artifact digest、最终 trust/ledger entry 或任何由其产生的 head；先 hash 完整 raw plan，再生成 canonical authorization artifact，最后派生并 hash 绑定 plan/bundle/authorization digests 的 trust/ledger entries。activation bundle 将完整 pre-activation inventory、initial signed inventory/manifest、minimum fence-contract version 与 canonical domain identity set bytes/digest/epoch 逐字绑定；最终 activation entry 才绑定 trust revision/head。错序、自引用、重复 identity-set serialization、object/actor/idempotency/origin/floor/release 非空均由数据库 CHECK、Go/PostgreSQL/S3 decoder共同拒绝；`backup_recoverability=none` 也必须发布可验签的零恢复点 inventory。
- [ ] `PF-AC-011` record-platform admission readiness、LB 与 queue membership gate 对低合同版本或陈旧 heartbeat 的接纳数为 0；每个 API/worker 使用独立短 heartbeat，claim/lease 获取强制校验 admission。低版本进程可以为无关监控域启动，但旧 `/ready` 200 不能替代受保护域 exact-204 admission。只有 deletion replay watermark 与 fence projection 连续追平 fresh witness head 后才能放行，已观察 minimum fence-contract version 永不下调。
- [ ] `PF-AC-012` add/rotation/retire/compromise/remove 的授权、双 key/离线 approver证明、依赖库存和 ack-loss 语义均有真实 PostgreSQL + full witness 回归；approval-policy rotation 分别满足 current/candidate threshold，每个 candidate-only 新 approver key 都提供 candidate-scope possession signature，同一 envelope 跨 scope 双计数为 0；提前删除仍被 manifest/activation 依赖的 public key 成功数为 0。
- [ ] `PF-AC-013` planned single-domain replacement 与丢失 app/ledger/witness/recovery 域的受支持恢复均通过 `domain identity rotate` cutpoint/failure matrix：candidate epoch 只增 1、恰一 member 改变；challenge 与 preparation 分别由 witnessed-current 与 candidate governance threshold signature set 认证，offline signer 无数据库/网络调用，prepare governance 私钥读取数为 0。完整 sealed challenge/preparation 能从 typed intent 的 primary/full witness 重建；唯一 `ValidateDomainRotationIntentV1` 在 plan/apply/import/resume/primary/PostgreSQL-witness/S3 put+readback 递归验证 canonical re-encode、所有 body/wrapper digest、policy/threshold/Ed25519 signature、排序、possession 与 epoch/domain/mutation context，并共享同一负向 corpus。identity-set body、独立 primary receipt、显式绑定其 digest 的独立 witness receipt、intent/cutover bundle 与 transfer object 都有闭合 decoder/schema，primary 丢失时 full witness 仍可重建三者。current approval、candidate possession/threshold attestation、genesis chunked copy、planned dual-write 或通过独立 unreachable draft/governance-sign/seal/resume 生成的 disaster proof、drain/retirement/final proof 均逐字可验；LB/queue/copy-replay 的初始与 continuation drain receipt 保存完整 signed snapshot wrappers、policy 与 fresh inventory，删除本地 snapshot 后仍能从 full witness 验证。两个transfer exporter与recovery-request exporter都显式接收 `--recovery-signing-key PATH`，派生 signer 与policy/intent中witnessed current signer不一致时输出字节数为 0。typed recovery request必须先完成primary/full-witness immutable publication，再携带purpose-bound current signature与完整2+3 receipts经FD交给candidate；无签名、自哈希或未发布FD成功数为0。`cutover_apply` request的required head必须是witnessed `Cutover` receipt，且其`PreCutoverReceiptChainHead`绑定内嵌`candidate_import_revoked`。`ObjectStart/ObjectChunk/ObjectEnd` 对 24 MiB plan、20 MiB bundle 与 8 MiB inventory 的 exact-max 均成功，一字节超限、gap/overlap/offset/index/count/digest 错误均在提交前失败。receipt 只能按 `DrainContinuation* → CandidateImportApplied → CandidateImportRevoked → Cutover` 前进，15 个 kind 在 Go/primary SQL/PostgreSQL witness/S3 decoder 一致且仅 continuation 可重复；applied 必须先 current-side full-witness/readback，随后才可显式撤销 import 并生成 binding revocation，cutover 只能由 current signed export、candidate apply、current `resume --receipt-fd FD` 验签并 full-witness 回流，candidate 写 current recovery-control 次数为 0。Final full-witness 后必须先撤销cutover credential并由current primary/full-witness/readback revocation，再销毁candidate receipt key/bundle/workspace与AEAD/nonce key material，再witness purge/workspace-zero；completion显式绑定最终receipt-chain、policy-head generation/digest及publication receipts后才可置complete。revocation readback前销毁成功数为0。旧单体 challenge/cutover 命令、旧域重现、candidate far tail/缺口、copy/teardown receipt 不全、旧配置回滚、profile 改变或跳 epoch 的 admission/complete 成功数均为 0；current full witness/注册恢复源不足以重建时没有 override，只保持关闭。
- [ ] `PF-AC-014` generation-1 candidate-control prepare policy由read-only draft创建mutation ID，且两份显式 public descriptor、witnessed current recovery-request signer descriptor与≤30天immutable deadline缺一即在stat前失败；所有descriptor validity必须覆盖deadline。`domain governance sign --purpose candidate-control-policy --scope current|candidate`是policy唯一离线签名入口；经current/candidate两个governance threshold离线签名、seal、current primary/full-witness publish后，challenge draft先以完整body做monotonic challenge-start CAS并完成typed 2+3 fence publication，随后才可输出且逐字内嵌该policy。same-phase renewal和import/cutover/cleanup phase advance都绑定完整previous policy并逐字继承deadline及nonce/cleanup/request-signer descriptors，重新传入、byte drift或跳phase成功数为0；每代在使用前完成publish/readback。完整descriptors在policy/challenge fence/preparation/intent/plan间全等。所有candidate命令只接受由policy-pinned current signer签名且完成2+3发布、包含完整publication receipts的typed recovery-request FD，purpose/phase cross-use成功数为0。pre-intent abandon与intent CAS只能一个获胜；无sealed preparation时也可按witnessed prepare policy及可选challenge fence清零，authorization和内嵌完整signed purge/workspace-zero receipts的completion均完成2+3 readback且不增加rotation kind。import/cutover revoke receipt在current append/full-witness/readback前，receipt key/bundle/workspace销毁数为0。S3/filesystem/PostgreSQL并发nonce reservation对同`(mutation,key identity,nonce)`只有一个winner，ack-loss exact-match且不同artifact不能复用。nonce signer只能签两类encrypted-payload reservation，candidate receipt signer只能签import/cutover/revocation phase receipt，cleanup verifier只能签purge/workspace-zero，current request signer只能签`candidate_recovery_request_v1`，四者cross-use成功数为0；AEAD/wrapped-DEK/nonce destruction evidence不另签名并由purge wrapper覆盖。nonce private key只在prepare/import/cutover暴露signer，revoke不打开，abandon/cleanup仅identity-check/destruction且sign调用数为0。六个encrypt/decrypt/destroy命令的local-key XOR KMS parser/open、descriptor/evidence和最终inventory exact XOR；Final canonical body的policy head+full verifier descriptor、completion的policy-head generation/digest+primary/witness publication receipts在Go/primary/PostgreSQL-witness/S3全等。最终candidate-control live/version/delete-marker/multipart与phase credential authority、AEAD/nonce-signing key残留均为0。
  recovery-request purpose/phase/arm固定为`abandon→prepare/authorization`、`import→import/intent+transfer-start`、`revoke_import→import/intent+import-applied`、`cutover_apply→cutover/intent+import-revoked+cutover-head+command`、`revoke_cutover→cleanup/intent+final-proof`、`cleanup→cleanup/intent+final-proof+cutover-revoked`；nil/both/wrong arm或wrong receipt head成功数为0。purge context只能exact-one内嵌完整sealed preparation或含typed primary/witness receipts的witnessed abandon authorization，开放kind+digest成功数为0。
- [ ] `PF-AC-015` recovery signing key 使用 bounded、regular、no-follow、严格 0400 loader；center 只在 witnessed active key 与完整 runtime gate 成立后签新 inventory/manifest，revision 0 时绝不 seed。永久删除幂等性改用 preview 阶段服务端 CSPRNG 签发的 canonical 256-bit `DeletionRequestTokenV1` 与 domain-separated public commitment：raw token 不持久化，低熵/错格式/跨 scope 重放拒绝，ack-loss 重试无需历史 HMAC keyring或第 65 次轮换迁移。
- [ ] `PF-AC-016` lease/guard/member/content-epoch详细引用在闭合终态后24小时内清理，purge/import/export/processor/backup/restore详情在verified receipt后最多30天去关联；telemetry固定`raw_event_delete_at=min(captured_at+configured_sink_ttl,captured_at+30d)`并覆盖在线/归档/备份/复制目标。notification/delivery固定`product_expires_at=min(created_at+configured_ttl,created_at+180d)`，永久删除立即清content与record/revision/object/recipient/integration/provider-message/channel关联，verified purge后claim/lease 24h、identity-free detail最多30d。forensic restore不越过source recoverable-until。mutation只在kind-specific complete proof成立后压缩。八个registry roots以exact-one `RetentionSurfaceAddressV1`逐row exact join；21个lifecycle clock template与24个purge participant程序行唯一、owner/introduced顺序合法，live participant set与Go/Web executable binding全等。每个policy stage独立绑定participant、exact `(surface kind,action,required proof kind+schema set)` capability tuple、survivor与typed forbidden residue；row-level独立数组不得授权Cartesian product，未绑定stage不隐式转换，每个template stage全局至少一个reverse consumer。class覆盖live authority/derived、draft、managed client、immutable/minimal/operational/recoverability/storage/ephemeral；正常product text/binary/canonical/display/filename/safe URL只在owner存续时合法，owner delete后survivor none。`IndexedDBDraftBufferV1` exact store/key/codec、≤256KiB/24h且无附件/证据bytes；offline只披露ack-or-expiry，其他browser storage内容命中为0。`cryptographic_challenge`与`encrypted_key_material`不能伪装成digest/content。candidate policy/envelope/payload/nonce/key-destruction和managed-file terminal均一对一具名schema；S3 multipart与phase credential authority分别由abort/revoke action及zero proof闭合。`RequiredForbiddenAfterTransitionV1`按class+semantic+lifecycle+stage+survivor生成每个适用stage唯一必需集合，persisted值必须全等；任意enum的单项遗漏/额外值均在build/readiness失败。逐row逐deadline corpus覆盖product text/binary/canonical/display、filename、safe URL、path、free-text、credential及record/origin/actor/governance/operation/recovery-source/external-delivery identity，在PostgreSQL、primary/full witness、S3 body/key/metadata/control、filesystem和managed client非法位置命中为0；长期allowlist不含内容、路径或自由文本。
- [ ] `PF-AC-017` `RetentionAcceptancePolicyV1`、`ChildSurfaceOwnerEntryV1`、`ChildRetentionSourceClaimV1` 与 `SignedChildRetentionMergeAcceptanceV1` 均有 canonical golden/negative corpus。golden 固定 repository ID `1232752877`、issuer App ID `15368`、genesis threshold 2、`gapk-sha256-*` key IDs、merge order与 raw-byte-sorted 五 contexts；独立 PG16 contexts 不得出现在该集合。acceptance/genesis authority files 必须成对 canonical provision，acceptance key rotation/history只能通过完整旧 policy threshold 链验证并以 opaque verified-history capability 交给 caller。PR claim 对 exact base/source tree 是确定性的且不读取私钥；protected-main acceptance 对双亲 merge commit、highest-attempt required-check snapshot、owner entry、生产输入、registry `before+delta=after` 与重扫 inventory 全等后才签名。wrong tree、stale/newer-nonsuccess check、错误/过期/retired-for-signing/compromised/未列入 policy 的 key、workflow/scanner/signer digest 漂移、signature replay、previous-acceptance gap、registry replacement/removal、metadata nested/empty/noncanonical path、metadata PR 多余文件、partial authority replacement 或首轮 `no_new_surface` 的成功数均为 0。签名文件完整内嵌 source claim，并只通过 metadata-only PR 入库；第 N+1 个 child 在第 N 份 metadata PR 合入前无法取得有效 base claim，Task 11 无法预签自身 acceptance 或替其他 owner 增补 surface。
- [ ] `PF-AC-018` 新增 config、独立 migration runner、worker、router/bootstrap/deploy 变更均有单元、真实 PostgreSQL、Compose 与 systemd 验证。
- [ ] `PF-AC-019` `make verify-go` 与独立 ledger/recovery/witness fresh + repeated migration tests通过。

## Out of Scope

- 不增加通用用户管理、匿名分享或逐用户临时 ACL。
- 不在本任务实现记录永久删除的业务内容 purge；子任务 2/3/10 提供适配器，子任务 11 做跨存储终验。
- 不以 KMS/Vault 逐记录密钥销毁替代方案 A。

## Execution Gate

- 规划源保持 `planning`；已启动的隔离实施分支在本次方案 A 的 PRD/design/implement 书面审阅通过前保持冻结。审阅通过后恢复同一子任务，不重复启动父任务或其他子任务。
