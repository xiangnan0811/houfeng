# S3 profile run 2026-08-21

MinIO `minio/minio:RELEASE.2024-12-18T13-15-44Z` on host network.
`TMPDIR=/tmp`. Any `--- SKIP:` is a failure.

## Integration `--profile s3`

Failed. Two returned owning-child defects; Child 11 did not rewrite them.

1. `TestPostgresIntegrationRecordPortabilityDeletionPurgesOwnedRowsKeepsTombstonesAndReplays`
   same Alpine/musl `0058` `blob_key` CHECK as local
   (`invalid regular expression: invalid repetition count(s)`).
2. `TestPortabilityImportStagingMinIOConformance` and
   `TestPortabilityLeasedBlobStoreStageImportMinIORoundTrip`
   `error = invalid Blob request` (attachments/portability domain).
   Child 11 only supplies endpoint/keys/bucket; it does not change
   `NewS3BlobStore` or import staging keys.

`recordbackup` / `recordrestore` packages were ok before store/portability failed.

## Recovery `--profile s3 --all`

The profile report was written:

```json
{"format":"houfeng-record-profile-report/v1","profile":"s3","permanent_delete":"disabled","missing":["deletion.record_markdown_client","deletion.record_comparison"]}
```

The first run then exited 1 because Docker-root MinIO files blocked
`rm -rf` of the workspace. Cleanup is now `rm -rf "$workspace" || true`
so a green suite is not overturned by teardown permissions.
